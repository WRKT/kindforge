# Velero Toolbox

Velero Toolbox is a set of tools used to interact with the Velero API on a Kubernetes cluster as Pod.

This repository contains the velero toolbox Docker image and its helm chart to easily deploy on a Kubernetes cluster.